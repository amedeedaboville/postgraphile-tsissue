Graphile issue repro
---
Simply has `typescript` and `postgraphile` installed in package.json, and imported in `index.ts`

You can install with `npm i`, then run `node_modules/typescript/bin/tsc` from this folder.
